import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dailyspark.app',
  appName: 'DailySpark',
  webDir: '.output/public'
};

export default config;
